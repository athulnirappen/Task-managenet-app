services:
  - type: web
    name: task-management-api
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm run deploy
    envVars:
      - key: NODE_ENV
        value: production
